program prueba
    [* En este programa se comprueba como tanto el and, el or y el not estan bien definidos y su traduccion es la correcta*]
    int p;
    int a;

    {
        p=2;
        a=10;
        if p < 10 or p > 1 {
            if a >= 0 and a > p{
                a = a - 1;
            };
            p = p + 1;
            if not p > 5 {
                p = p + 5;
            };
        };

    }