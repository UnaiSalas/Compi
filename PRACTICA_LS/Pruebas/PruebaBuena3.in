program prueba

    int p;
    int a;

    {
        p=2;
        a=10;
        if p < 10 or p > 1 {
            if a >= 0 and a > p{
                a = a - 1;
            };
            p = p + 1;
        };

    }